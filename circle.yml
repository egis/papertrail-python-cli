machine:
  python:
    version: 3.5.1

dependencies:
  post:
    - pip install -U -r requirements.txt
    - python setup.py sdist
    - sudo pip install twine
    - twine upload dist/* 

test:
  override:
    - echo ok